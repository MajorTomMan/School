## 修改点

- 新增答题会话、答案提交、跳过、提示和解析查看等学习事实的 Room 持久化，支持应用重启后恢复当前题目与完整作答历史。
- 新增题目结果、题组结算、掌握度证据、掌握度状态和掌握度曲线快照表，所有统计均由持久化事实重新推导。
- 新增一次性事务结算服务，在同一事务内生成正确率、做错题数、错误提交次数和掌握度更新；重复结算只返回原结果，不重复应用证据。

## 修复点

- 在持久化边界拒绝已结束会话继续作答、答对后重复提交、题组外题目和不连续的提交序号，避免界面异常污染统计。
- 结算时校验课程内容修订号，防止课程更新后使用新题目定义结算旧会话；任何结算失败都会整体回滚。
