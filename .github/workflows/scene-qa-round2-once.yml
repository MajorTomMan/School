name: One-time Scene QA Round 2

on:
  push:
    branches: [agent/scene-qa-round2]
    paths:
      - ".github/scripts/scene_qa_round2.py"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/scene-qa-round2
      - name: Apply second scene QA patch
        run: python3 .github/scripts/scene_qa_round2.py
      - name: Commit changes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add app version.properties .release-notes/current.md
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "fix: refine remaining lesson visualizations"
            git push origin HEAD:agent/scene-qa-round2
          fi
