<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.School"
        android:usesCleartextTraffic="true">
        <provider
            android:name=".data.material.PrebuiltTextbookBootstrapProvider"
            android:authorities="${applicationId}.prebuilt-textbooks"
            android:exported="false"
            android:initOrder="100" />

        <provider
            android:name=".data.material.PrebuiltCatalogBootstrapProvider"
            android:authorities="${applicationId}.prebuilt-catalog"
            android:exported="false"
            android:initOrder="95" />

        <provider
            android:name=".data.curriculum.MasteryTrendBootstrapProvider"
            android:authorities="${applicationId}.mastery-trends"
            android:exported="false"
            android:initOrder="90" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.update-files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/update_file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>

</manifest>
